<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>面了个壁</title>
    <style type="text/css" media="screen">
      p { padding-inline: 1em; }
      p.msg { font-size: 1.5em; color:black; }
      p.reply { font-size: 1.2em; color: #999; }
    </style>
  </head>

  <body>
    <p class="msg"><%= message %></p>
    <p class="reply">
      <pre><%= reply %></pre>
    </p>
  </body>
</html>
